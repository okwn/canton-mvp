# Canton MVP Web — Environment template
# Copy to .env.local. Never commit secrets.
# See docs/security-model.md

# API base URL (must match API server)
NEXT_PUBLIC_API_URL=http://localhost:8080/api/v1
