/**
 * @canton-mvp/daml-models
 *
 * Daml contract models and generated TypeScript types.
 * Run `pnpm run build:daml` then `pnpm run build:codegen` to generate types.
 *
 * Modules: PartyProfile, AppRegistry, AppConnection, TokenIntent, QuoteRequest,
 * SwapIntent, SettlementInstruction, TransferApproval, AuditEvent.
 */

export const PACKAGE_ID = "canton-mvp-models";
